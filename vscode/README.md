Some useful configuration options for launching VS Code Node.js debugger (locally).
Before using it, change all properties, setting them according to your project needs.

Put it inside ./vscode/launch.json
